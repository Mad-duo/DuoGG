import React from 'react';
import { Badge } from 'react-bootstrap'

const Root = () => {
  return (
    <h1>
      Hello, Start Kit <Badge variant="secondary">New</Badge>
    </h1>
  );
};

export default Root;